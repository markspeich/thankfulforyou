{
  "name": "thankfulforyou",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node tools/build_static.mjs",
    "setup:worktree": "node tools/setup_worktree_env.mjs",
    "start": "node tools/setup_worktree_env.mjs && node --watch tools/dev_server.mjs",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "vitest run",
    "test:db": "node tools/setup_worktree_env.mjs && npx supabase db reset --local && vitest run --config vitest.db.config.js",
    "test:e2e": "playwright test",
    "test:e2e:preview": "node tools/run_playwright.mjs preview"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vitest": "^4.1.6"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@supabase/supabase-js": "^2.49.8",
    "a11y-dialog": "^8.1.5"
  }
}
